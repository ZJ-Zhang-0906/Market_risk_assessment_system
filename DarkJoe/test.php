<?php

    // // 删除旧的 JSON 文件（如果存在）
    // $jsonFilePath = 'DarkJoe\data.json';
    // if (file_exists($jsonFilePath)) {
    //     unlink($jsonFilePath); // 删除文件
    // }

    // $scriptPath = 'DarkJoe/pyy/main.py';